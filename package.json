{
  "name": "whatsapp-recipe-bot",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsApp Recipe Bot - FamFood Subdomain Integration",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0",
    "vitest": "^1.1.0"
  },
  "packageManager": "pnpm@8.15.0"
}